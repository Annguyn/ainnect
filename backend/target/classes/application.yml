spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ainnect?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: annguyen199
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: info
    org.hibernate.orm.jdbc.bind: info

# JWT Configuration
jwt:
  secret: mySecretKeyForAinnectApplicationThatIsVerySecureAndLong
  expiration: 86400000 # 24 hours in milliseconds
  refresh-expiration: 604800000 # 7 days in milliseconds